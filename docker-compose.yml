services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: garagebrain
      POSTGRES_USER: garagebrain
      POSTGRES_PASSWORD: garagebrain
    ports:
      - "5432:5432"
    volumes:
      - garagebrain_pg:/var/lib/postgresql/data

volumes:
  garagebrain_pg:
